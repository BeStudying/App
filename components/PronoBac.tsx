import * as React from 'react';
import {SafeAreaView} from 'react-native';
import {DrawerScreenProps} from "@react-navigation/drawer";

export default function PronoBac({route}: DrawerScreenProps<any, "Prono'Bac">) {
    return (
        <SafeAreaView>

        </SafeAreaView>
    );
}